### 记录文档，为己所用

#### 远程操作

```python
git相关：
or create a new repository on the command line
echo "# git20181127" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/ziqiwu/git20181127.git
git push -u origin master
…or push an existing repository from the command line
git remote add origin https://github.com/ziqiwu/git20181127.git
git push -u origin master
…or import code from another repository
You can initialize this repository with code from a Subversion, Mercurial, or TFS                project.
Import code

准备工作：本地git已经执行了git init -- git add -- git commit
第一步：git remote add origin https://github.com/ziqiwu/git20181127.git --
       添加GitHub的仓库源地址，将该本地仓库与该GitHub源仓库关联 --
       该操作只用执行一次
第二步：git push -u origin master  
	   将本地仓库内容推送到远程仓库;
       其中master是仓库下的分支, 第一次push 需要加上-u参数，执行以下命令来进行推送(push), 过                程中可能需要输入GitHub的用户名和密码
       git push origin master 
       以后push就不需要再加-u参数
	   

        

```

#### 新建仓库

```
第一步：进入https://github.com/ziqiwu
第二步：点击Repositories
第三步：点击NEW
```

